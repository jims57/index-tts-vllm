work, good. now:
a. the @api.py will be exposed to the aliyun alb, which will send health check periodically like this @aliyun-head.log , i want to make should@api_server.py is fully ready(i mean it can generate tts normally, mostly it will takes about many seconds to be ready, since model takes time to run), make the health check @api.py#L110-116 unhealthy(if unhealthy alb won't send users request to the api @api.py ), until the @api.py can actually get tts streams from   @api_server.py . you can check @api_server.py every 5s to send tts request to @api_server.py , if it is ok(true tts streams from   @api_server.py ), and then @api.py#L110-116 will response healthy to alb, so that our users can actually get truly tts streams from   @api_server.py .  after @api.py can get the true tts steams from   @api_server.py for the first time, it don't need to periodically check the @api_server.py .